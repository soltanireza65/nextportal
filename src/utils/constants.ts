import { SiteConstants } from 'interfaces/base';

const siteConstants: SiteConstants = {
	siteTitle: 'رازی پرس',
	siteURI: 'http://localhost:3000',
	imageDomains: ['media.behsoud.com']
};

export default siteConstants;
